/** Event emitted after the live job search changes the URL without a page
 * navigation. Persistent search controls use it to resync URL-backed state. */
export const JOB_SEARCH_CRITERIA_CHANGED_EVENT =
  "smarthire:job-search-criteria-changed";
