import "../../frontend/styles/workspace.css";
import "../../frontend/styles/responsive.css";
import "../../frontend/styles/recruiter-workspace-full.css";

export default function RecruiterEntitlementLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
