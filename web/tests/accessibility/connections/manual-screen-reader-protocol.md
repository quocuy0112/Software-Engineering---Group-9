# Connections Screen Reader Protocol

Using NVDA/Chrome and VoiceOver/Safari, traverse `/connections` and the administrator proposal screens by keyboard. Verify headings, live errors/status, proposal state text, accept/decline labels, disconnect confirmation, notification read state, account search results, focus visibility, and that color is never the sole state signal. Record platform, browser, result, and issue reference.
