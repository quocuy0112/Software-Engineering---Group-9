import { readFile, readdir } from "node:fs/promises"; import { resolve } from "node:path"; import { describe,expect,it } from "vitest";
async function files(directory:string):Promise<string[]>{return (await readdir(directory,{withFileTypes:true})).flatMap((entry)=>entry.isDirectory()?[]:[resolve(directory,entry.name)]);}
describe("browser state policy",()=>{it("keeps the shared UI store allowlisted and rejects browser secret persistence",async()=>{const store=await readFile(resolve(process.cwd(),"src/features/shared/stores/ui.ts"),"utf8");expect(store).not.toMatch(/password|token|secret|code|cookie/i);const roots=await Promise.all(["src/components","src/features","src/app"].map(files));for(const file of roots.flat()){const source=await readFile(file,"utf8");expect(source).not.toMatch(/(?:localStorage|sessionStorage)\.setItem\s*\([^,]*(?:password|token|secret|code|cookie)/i);}});});
