"use client";
import { useEffect,useRef,useState } from "react";
import { useRouter } from "next/navigation";
import { TWO_FACTOR_GENERIC_ERROR } from "@/features/identity/schemas/two-factor";
export function TwoFactorChallenge(){const router=useRouter(),input=useRef<HTMLInputElement>(null),[code,setCode]=useState(""),[status,setStatus]=useState(""),[busy,setBusy]=useState(false);useEffect(()=>{input.current?.focus();return()=>setCode("");},[]);async function submit(e:React.FormEvent){e.preventDefault();if(busy)return;setBusy(true);setStatus("");const sent=code;setCode("");try{const r=await fetch("/api/identity/two-factor/complete",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({factor:"totp",code:sent})});if(!r.ok){setStatus(TWO_FACTOR_GENERIC_ERROR);input.current?.focus();return;}setStatus("Verification complete.");router.replace("/settings/sessions");router.refresh();}catch{setStatus(TWO_FACTOR_GENERIC_ERROR);}finally{setBusy(false);}}
return <main className="auth-shell"><form onSubmit={submit} noValidate aria-busy={busy} className="auth-card"><h1>Two-factor verification</h1><p>Enter the six-digit code from your authenticator app.</p><div className="field"><label htmlFor="totp-code">Authentication code</label><input ref={input} id="totp-code" name="code" type="text" inputMode="numeric" autoComplete="one-time-code" pattern="[0-9]{6}" maxLength={6} value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,"").slice(0,6))} aria-describedby="two-factor-status" required /></div><button type="submit" disabled={busy||code.length!==6}>{busy?"Verifying…":"Verify"}</button><p id="two-factor-status" role="status" aria-live="polite">{status}</p><a href="/login">Back to sign in</a></form><style jsx>{`.auth-shell{width:100%;max-width:28rem;margin:auto;padding:1rem;overflow-x:hidden}.auth-card{min-width:0}.field input{box-sizing:border-box;width:100%;font-size:1rem}@media(prefers-reduced-motion:reduce){*{scroll-behavior:auto!important;transition:none!important}}`}</style></main>}
