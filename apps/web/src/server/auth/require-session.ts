import "server-only"; import { randomUUID } from "node:crypto"; import { BetterAuthSessionGateway } from "./identity/better-auth-session-gateway"; import { SessionService } from "@/server/services/identity/session-service"; import { PrismaAuditRepository } from "@/server/repositories/audit/prisma-audit-repository";
export async function requireSession(headers:Headers,now=new Date()){const gateway=new BetterAuthSessionGateway();const current=await gateway.current(headers);if(!current)return null;const valid=await new SessionService().validate(current.session.id,current.user.id,now);if(!valid){await gateway.signOut(headers).catch(()=>undefined);await new PrismaAuditRepository().append({occurredAt:now,actorType:"user",actorUserId:current.user.id,actorSessionId:current.session.id,action:"session.revoked",targetType:"session",targetId:current.session.id,result:"DENIED",correlationId:randomUUID(),context:{reason:"policy_enforcement"}}).catch(()=>undefined);return null;}return {userId:current.user.id,sessionId:current.session.id};}
