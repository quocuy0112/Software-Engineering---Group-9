import { validateSameOrigin } from "@/lib/security/csrf"; import { validCsrfProof } from "@/lib/security/csrf-proof"; import { serverEnvironment } from "@/lib/env/runtime"; import { requireSession } from "@/server/auth/require-session"; import { BetterAuthSessionGateway } from "@/server/auth/identity/better-auth-session-gateway";
export async function POST(request:Request){const current=await requireSession(request.headers);if(!current)return Response.json({message:"Signed out."});if(!validateSameOrigin(request,serverEnvironment.NEXT_PUBLIC_APP_URL)||!validCsrfProof(current.sessionId,request.headers.get("x-csrf-token")))return Response.json({message:"Request rejected."},{status:403});const upstream=await new BetterAuthSessionGateway().signOut(request.headers);const headers=new Headers({"Content-Type":"application/json","Cache-Control":"no-store"});for(const cookie of upstream.headers.getSetCookie())headers.append("Set-Cookie",cookie);return new Response(JSON.stringify({message:"Signed out."}),{headers});}
