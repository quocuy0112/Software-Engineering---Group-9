POSTGRES_DB=smarthire
POSTGRES_USER=smarthire
POSTGRES_PASSWORD=replace-with-a-generated-local-password
POSTGRES_PORT=55432

# Local/test accepts only the direct loopback marker at 0 trusted proxy hops.
# Production must configure the actual reverse-proxy chain with a value >= 1.
AUDIT_TRUSTED_PROXY_HOPS=0
